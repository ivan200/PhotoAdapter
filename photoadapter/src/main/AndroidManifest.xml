<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-sdk tools:overrideLibrary="com.otaliastudios.cameraview, androidx.camera.video, androidx.camera.core, androidx.camera.view, androidx.camera.camera2, androidx.camera.lifecycle, androidx.annotation.experimental, androidx.exifinterface" />

    <supports-screens
        android:anyDensity="true"
        android:xlargeScreens="true" />

    <application
        android:supportsRtl="true"
        android:allowBackup="true"
        tools:ignore="RtlEnabled">
        <activity
            android:theme="@style/AppThemePhoto"
            android:name="com.ivan200.photoadapter.CameraActivity"
            android:exported="false"
            android:label="Photo" />
    </application>
</manifest>

